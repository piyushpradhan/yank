[package]
name = "smart-clipboard-app"
version = "0.4.5"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "smart_clipboard_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri-plugin-global-shortcut = "2.3.1"
tauri-plugin-clipboard-manager = "2.3.2"
rusqlite = { version = "0.39.0", features = ["bundled"] }
regex = "1.12.3"
arboard = "3.6.1"
chrono = "0.4.44"
once_cell = "1.21.4"
reqwest = { version = "0.13.2", default-features = false, features = ["json", "rustls"] }
tauri-plugin-store = "2.4.2"
tokio = { version = "1.52.1", features = ["rt", "macros", "sync", "time"] }
tauri-plugin-autostart = "2"
tauri-plugin-single-instance = "2"
fastembed = { version = "4", default-features = false, features = ["ort-download-binaries", "hf-hub-rustls-tls"] }
windows = { version = "0.60", features = ["Win32_Foundation", "Win32_UI_WindowsAndMessaging"] }

