# Umami Cloud — set in Vercel Project Settings, or copy this to .env locally
# Get the website ID from https://cloud.umami.is after creating a site.
PUBLIC_UMAMI_WEBSITE_ID=
# Optional: override the script URL (default: https://cloud.umami.is/script.js)
# PUBLIC_UMAMI_SRC=
